import { AnciennetePipe } from './anciennete-pipe';

describe('AnciennetePipe', () => {
  it('create an instance', () => {
    const pipe = new AnciennetePipe();
    expect(pipe).toBeTruthy();
  });
});
